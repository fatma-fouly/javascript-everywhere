Q1- Let allows me to Reassign to new value but const not

- but even that in array i can use push with const because i can change in array not reassign the value.

Q2-Premitive types
1-string
2-number
3-boolean
4-undefined
5-Bigint
6-null

Q3-Null returns object cause an old bug in javascript
how i check? - by using strict equality === to check value and type together

Q4-FalsyValues 1-null 2-(-0) 3-0 4-"" 5-false 6-NAN 7-undefined 8-0n

Q5-=== Vs ==
=== chech the type and value but == just check the value
EX: const x = Number(5);
if (x == "5"){
console.log("true")}
the accurate answer will be false bbut here js will neglect the type of value and consider it true.

Q6- with ?? we use "default" if before ?? null or undefined
and withh || we use "default" if before || one of the falsy values.

Q7-five loops
1-for loop => if i want loop in specific range
2-while loop => if i want loop whithin specific condition aand stop looping if the condition not done.
3-Do-while => when i want to execute the condition at least first one
4-for of => when i want loop in array values
5-for in => when i need to loop of keys in object

Q8-
